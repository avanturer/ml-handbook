# Приёмочный экзамен: сводный список работ

**Дата сведения:** 2026-08-01
**Источник:** шесть независимых отчётов интервьюеров по секциям `classic-ml`, `dl-nlp`, `llm`,
`recsys`, `mlops`, `ab-sysdesign`. Суммарно проверено 234 вопроса из банка
`.handbook/research/questions-*.md`.

**Итог после сведения: 46 работ — P0: 14, P1: 27, P2: 5.**

Арифметика: 51 пункт в отчётах (33 «СЛАБО» + 18 «НЕТ») → −4 после дедупликации →
−1 после отсева ложного срабатывания → **46**.

---

## Методологическая оговорка

Читайте её раньше списка: приоритеты здесь — редакторское суждение, а не измерение.

**Что проверялось и чем.**

- **Все 18 пунктов «НЕТ» проверены `ripgrep`-ом по `docs/`** (142 файла, 14 МБ) —
  требование «хотя бы половину» перевыполнено. Искал русскими и английскими терминами
  сразу, включая синонимы и аббревиатуры: например, для «предсказательного интервала» —
  `предсказательн|prediction interval|конформн|conformal|квантильн[а-я]* регресси`,
  для outbox — `outbox|Debezium|транзакционн[а-я]* исходящ`.
- **Спот-проверено 29 из 33 пунктов «СЛАБО»** тем же способом. Не проверял грепом четыре
  пункта, где претензия относится к *качеству* уже написанного текста, а не к наличию слов.
- **Целевые секции читались глазами** там, где грep давал совпадения: `04-nlp/05-nlp-tasks-and-metrics.md`
  §2.2, `12-coding/02-numpy-pandas.md` §15, `08-big-data/05-streaming.md` §13,
  `11-system-design/05-case-rag-assistant.md` §L0-бейзлайн.

**Где проверка слабая — читайте как границы доверия.**

1. **Грep находит слова, а не ответы.** Если термин в книге есть, это ещё не значит, что на
   вопрос можно ответить прочитанным. Обратное надёжнее: если термина нет ни в одном из 5–8
   написаний — дыра реальная. Поэтому отсев ложных срабатываний здесь достоверен, а
   подтверждение дыр — тем более, но *переоценка* дыр («СЛАБО» там, где на самом деле «ЕСТЬ»)
   могла остаться незамеченной.
2. **Утверждения «СЛАБО» о глубине не перепроверялись целиком.** Чтобы честно опровергнуть
   «этот вывод недостаточно подробен», надо прочитать главу подряд; я читал только окрестности
   указанных параграфов. Здесь я доверился интервьюерам.
3. **Раздел «Что закрыто уверенно» в отчётах не проверялся вообще.** Если интервьюер ошибочно
   счёл тему закрытой, эта дыра в списке не появится. Это главный систематический риск сводки.
4. **Частота вопросов на реальных собеседованиях не измерялась.** Приоритеты P0/P1/P2 опираются
   на пометки грейда в банке вопросов, число независимых источников дампа и на то, насколько
   пусто в книге, — но это оценка, а не статистика. Спорные случаи помечены явно.
5. **Не проверялись** `code/`, `resources/`, `README.md` и материалы `.handbook/` — только `docs/`.
   Если разбор живёт в упражнении, а не в главе, я его не увидел (для читателя книги, впрочем,
   разница невелика: на собеседовании он вспоминает главу).
6. **Объём работ («абзац / раздел / глава») — оценка сверху по смыслу дыры**, не по замеру
   похожих кусков текста.

---

## Что оказалось на месте

### Полное ложное срабатывание (1 пункт снят)

**«Метриками multi-label классификации»** (отчёт `classic-ml`, «НЕТ» №2). Интервьюер искал в
`docs/02-classic-ml/04-metrics.md` и не нашёл — но тема полностью закрыта в NLP-части:

> `docs/04-nlp/05-nlp-tasks-and-metrics.md`, §2.2 «Многометочная классификация» (строки ~103–182)
> плюс вопрос в «Проверь себя» на строке 1145.

Там есть ровно то, что просил отчёт, и больше: почему $K$ независимых сигмоид и BCE вместо
softmax (с формулой лосса), таблица micro/macro/weighted/**samples**-F1, Hamming loss вместе с
численным контрпримером «тривиальная модель даёт 0,04», subset accuracy с оговоркой о строгости,
и сверх запрошенного — подбор порога на каждую метку с кодом. Не хватает единственного:
**mAP не назван**, и из `02-classic-ml/04-metrics.md` нет ссылки сюда.

Это не дыра, а перелинковка на 15 минут: одна строка-отсылка в `docs/02-classic-ml/04-metrics.md`
§2.3 и упоминание mAP в таблице метрик NLP-главы. В счёт 46 работ не включено.

### Частичные срабатывания: дыра есть, но уже́ и с якорем (4 пункта понижены в приоритете)

| Пункт отчёта | Что на самом деле уже есть | Что остаётся |
|---|---|---|
| `llm` «НЕТ» №2 — данные в весах и право на забвение | `docs/11-system-design/05-case-rag-assistant.md:361` прямо формулирует «удаление документа по требованию (право на забвение) в весах не реализуется» — как аргумент за RAG против дообучения | процессный ответ (псевдонимизация до обучения, привязка «пример → субъект», выходной фильтр, каскад удалений) — P1, не P0 |
| `mlops` «НЕТ» №7 — Outbox | `docs/08-big-data/05-streaming.md:1095` уже разбирает границу EOS при записи во внешнее хранилище: идемпотентный upsert либо 2PC-синк (Flink); дедупликация у потребителя есть в §13 | производящая сторона: сам transactional outbox и CDC-релей (Debezium) — P1, объём абзац |
| `ab-sysdesign` «СЛАБО» №3 — язык Пёрла | `docs/02-classic-ml/17-uplift-and-causal.md:625` уже содержит предупреждение про коллайдер по существу («её включение **создаст** ложную связь») | назвать вещи именами: do-оператор, backdoor criterion, DAG — терминология, а не механика |
| `ab-sysdesign` «СЛАБО» №5 — центрированное окно | `docs/08-big-data/02-sql-for-mle.md:529` даёт центрированную рамку `ROWS BETWEEN 3 PRECEDING AND 3 FOLLOWING` | приём «исключить текущую строку» + сама задача в тренажёре `12-coding/05-sql-drills.md` |

### Подтверждено грепом как реальная дыра (ноль совпадений во всей `docs/`)

`метод моментов`, `ANOVA` (кроме `fANOVA` в HPO и F-статистики первого шага для IV),
`MC-dropout`, `min-p`, `contrastive search`, `tree-of-thought`, `SelfCheckGPT`/семантическая
энтропия, `CAP`, `Greenplum`, `ключ дистрибуции`, `LSM`, `BRIN`/`GIN`, `MergeTree` как тема
(только упоминания ClickHouse как инструмента), `Terraform`/`IaC`/`GitOps` (кроме одного
упоминания ArgoCD в мониторинге), `autolog`, `outbox`, `consistent hashing`, `Saga`,
`unclean.leader.election`, `TFLite`/`Core ML`/`ExecuTorch`, `TextCNN`, `Bloom filter` как
устройство (только как готовый инструмент в Parquet), `диффузионные языковые модели`,
`HSTU`, `SENet`/`LHUC`/`MaskNet` в контексте ранжирования, `Swing`, `ESMM`, `xDeepFM`,
`эластичность` в ценовом смысле, `квантильная регрессия`, `конформное предсказание`.

---

## Дедупликация: что слито

| Слито | Почему это одна дыра |
|---|---|
| `llm` СЛАБО №4 + `ab-sysdesign` СЛАБО №4 — **LLM-агент text-to-SQL к legacy-базе** | буквально один и тот же вопрос из одной компании (PulsePoint, senior), пришедший из двух секций; обе претензии указывают на `05-llm/08-agents-and-tools.md` §3 и обе говорят «безопасность есть, содержания нет» → **работа №8** |
| `recsys` СЛАБО №4 + `recsys` НЕТ №2 — **scaling laws рекомендательных трансформеров** и **HSTU** | один сюжет под двумя именами: «растёт ли качество с параметрами и длиной истории вместо плато». ARGUS и HSTU — два примера одного тезиса, разводить их по разным главам бессмысленно → **работа №39** |
| `mlops` СЛАБО №2 + `mlops` НЕТ №4 — **перекос в MPP** и **ключ дистрибуции в Greenplum** | перекос в MPP *и есть* перекос по ключу дистрибуции; лечение в обоих пунктах одно и то же (сменить ключ, `DISTRIBUTED REPLICATED`, репартиционирование на загрузке) → **работа №12** |
| `mlops` СЛАБО №3 + `mlops` НЕТ №2 — **Index Only Scan** и **типы индексов / LSM** | обе претензии в один параграф `08-big-data/02-sql-for-mle.md` §10 и обе про физику индексов; покрывающий индекс без типов индексов не объяснить → **работа №13** |

Не слиты, хотя выглядят похоже: «ДИ vs предсказательный интервал» (работа №2, общая
статистика) и «инференс по коэффициентам регрессии» (работа №1, линейные модели) — разные
главы и разные вопросы, хотя вторая опирается на ту же $t$-конструкцию; два пункта про
позиционное кодирование (работы №18 и №43) — разные главы и разные грейды.

---

## P0 — без этого кандидат провалит секцию

Критерий: вопрос задают напрямую и часто, а в книге по нему ноль, обрыв ровно на месте отсева
или — худший случай — утверждение, из-за которого кандидат уверенно ответит неправильно.

| # | Работа | Файл | Объём |
|---|---|---|---|
| 1 | **Проверка гипотез о коэффициентах регрессии.** $H_0: w_j = 0$, $\mathrm{SE}_j = \sqrt{\hat\sigma^2[(X^\top X)^{-1}]_{jj}}$, $t$-статистика, $n-d$ степеней свободы и связь с VIF: раздувание $\mathrm{SE}_j$ ровно в $\sqrt{\mathrm{VIF}_j}$ раз. Ингредиенты в книге есть, собранного ответа на «два признака скоррелированы — что с коэффициентами и их ДИ» нет | `docs/02-classic-ml/02-linear-models.md` §5 (+ перелинковка с §6 VIF) | раздел |
| 2 | **Доверительный интервал против предсказательного.** ДИ покрывает параметр и стягивается как $1/\sqrt n$; ПИ покрывает следующее наблюдение, включает $\sigma^2$ и не стягивается. Обе формулы для регрессии. Плюс продовый выход: квантильная регрессия, quantile regression forests, конформное предсказание — **ни одного из этих трёх терминов в книге нет вообще** | `docs/01-math/03-statistics.md` §6 (перелинк из `02-classic-ml/07-bagging-random-forest.md:583` и `16-time-series.md`) | раздел |
| 3 | **ANOVA и F-статистика.** Разложение суммы квадратов на меж- и внутригрупповую, F-статистика, зачем омнибус перед попарными, почему в A/B чаще идут сразу к попарным с BH. Строка в сводную таблицу §10.1 и ветка «групп больше двух» в mermaid-дерево выбора критерия | `docs/01-math/03-statistics.md` §10.1 | абзац + строка таблицы + правка схемы |
| 4 | **Сводный список текстовых аугментаций.** EDA-набор, обратный перевод, парафраз через LLM, subword-регуляризация, шум опечаток, mixup на эмбеддингах — и правило «аугментация обязана сохранять метку» (NER/QA — не трогать спаны, тональность — не трогать отрицания). Сейчас — три слова в буллете CV-ориентированного раздела, остальное разбросано по четырём главам | `docs/04-nlp/01-text-representation.md` §15 | раздел (15–20 строк) |
| 5 | **MC-dropout.** Книга говорит только «на инференсе выключается полностью» — кандидат провалится на follow-up «а когда включают?». $K$ прогонов с включённым dropout как оценка эпистемической неопределённости, цена ×$K$ по латентности, калибровку надо проверять | `docs/03-deep-learning/02-training-dynamics.md` §8.1 | абзац |
| 6 | **Metric learning и смысл margin.** $\max(0, d(a,p) - d(a,n) + m)$; при $m=0$ лосс зануляется на любой разведённой тройке и обучение встаёт, при большом $m$ активны почти все тройки и оно расходится; связь margin с температурой InfoNCE, semi-hard mining. Слов «metric learning» и «triplet loss» в книге нет (только `TripletLoss` в списке литературы), при отличном контрастивном блоке рядом | `docs/04-nlp/02-embeddings.md` §11.2 | раздел |
| 7 | **Детекция галлюцинаций без контекста и эталона.** Все три рычага в книге требуют grounding. Нужна sampling-based линия: $N$ ответов при $T>0$ и взаимная согласованность утверждений (SelfCheckGPT), семантическая энтропия по кластерам эквивалентных ответов, оговорка про цену ×$N$ и применение на сэмпле трафика | `docs/05-llm/09-llm-evaluation.md` §5 (или `10-llm-safety-and-guardrails.md` §10.2) | раздел |
| 8 | **LLM-агент text-to-SQL к legacy-базе.** Schema linking при 500 таблицах (ретривал по описаниям, few-shot из истории запросов), семантический слой поверх непрозрачных имён, галлюцинации несуществующих колонок и джойнов, валидация до исполнения (`EXPLAIN`, dry-run, принудительный `LIMIT`, таймаут), метрика execution accuracy вместо сравнения текстов SQL. Сейчас — только безопасность | `docs/05-llm/08-agents-and-tools.md` §3 (мини-кейс рядом с §13) | раздел |
| 9 | **Моделирование watch time.** Взвешенная логистическая регрессия YouTube (позитив с весом $T_i$, шансы модели = ожидаемое время просмотра) и дебиасинг по длительности ролика (квантильная нормировка, D2Q), без которого «оптимизируем watch time» вырождается в «показываем длинные видео». Сейчас dwell time — строка в таблице целей и множитель в score fusion | `docs/10-recsys/08-neural-ranking.md` §6 | раздел |
| 10 | **CTR против CVR и ESMM.** CVR учится на кликнувших, а применяется ко всем показам — sample selection bias при данных на два порядка меньших; ESMM учит на полном пространстве показов pCTR и pCTCVR с общими эмбеддингами, pCVR берётся отношением. В книге на этот вопрос отвечено про другое — про отложенную конверсию | `docs/10-recsys/08-neural-ranking.md` §6 (перелинк из `11-system-design/08-case-ads-ctr.md` §10.3) | раздел |
| 11 | **`coalesce(n, shuffle=True)`.** Единственный случай дезинформации в сводке: книга дважды и уверенно утверждает «coalesce не шаффлит и только вниз», а банк вопросов содержит дословный follow-up «можно ли через coalesce увеличить число партиций». Кандидат ответит «нельзя» — и провалится | `docs/08-big-data/04-spark-tuning.md` §8 и `03-spark-fundamentals.md` §7 | абзац (правка утверждения в двух местах) |
| 12 | **Ключ дистрибуции и джойны в MPP + перекос вне Spark.** Local join против redistribute/broadcast — тот же shuffle, только невидимый; ключ выбирается по кардинальности и равномерности, а не по бизнес-смыслу; `DISTRIBUTED REPLICATED` для справочников; перекос упирает джобу в самый медленный сегмент, и лечится он сменой ключа, а не salting. Перекос разобран блестяще — но только для Spark | новая глава `docs/08-big-data/08-analytical-dbs.md` (или крупный раздел в `04-spark-tuning.md`) | раздел |
| 13 | **Индексы и физика хранения в SQL.** B-Tree (высота 3–4, бесполезен при низкой селективности), hash, GIN, BRIN, bitmap; LSM против B-Tree с write amplification; Index Scan / Index Only Scan / Bitmap Heap Scan в плане, покрывающий индекс и visibility map; MVCC, autovacuum и bloat; чем индексация OLAP отличается от OLTP | `docs/08-big-data/02-sql-for-mle.md`, новый раздел рядом с §10 | крупный раздел (2–3 стр.) |
| 14 | **Bloom filter.** Битовый массив + $k$ хеш-функций, отсутствие false negatives, $p_{FP} \approx (1-e^{-kn/m})^k$ и оптимальное $k = (m/n)\ln 2$ — то есть ответ на «как уменьшить FP», код на 20 строк, сравнение с `set` по памяти. В книге фильтр упоминается дважды как готовый инструмент, устройства нет. В банке — три вопроса подряд от одной компании | `docs/12-coding/04-algorithms.md` §3 + задача в §6 | задача с кодом |

---

## P1 — заметно усилит

| # | Работа | Файл | Объём |
|---|---|---|---|
| 15 | Метод моментов: приравнять выборочные моменты теоретическим, вывод для гамма/равномерного, расхождение с MLE на $[0,\theta]$ ($2\bar X$ против $\max X_i$), когда MOM берут | `docs/01-math/03-statistics.md` §4–5 | подраздел 300–400 слов |
| 16 | Таблица выбора варианта PCA: Incremental (`partial_fit`, потоковые данные), Randomized (первые $k \ll d$), Kernel (нелинейность + отображение новых точек, отсылка к Мерсеру, честное $O(n^2)$ по памяти) | `docs/02-classic-ml/12-dimensionality-reduction.md` §7 | раздел с таблицей |
| 17 | Приём фон Неймана: честный бросок из смещённой монеты, корректность из $P(\text{ОР})=P(\text{РО})$, ожидание $1/(p(1-p))$. Ложится в существующую рубрикацию §11 без перестройки, встречается в трёх источниках дампа | `docs/01-math/02-probability.md` §11.7 | подраздел (задача) |
| 18 | Почему нельзя прибавить к эмбеддингу номер позиции: неограниченная амплитуда, зависимость масштаба от длины, бесполезность разности индексов → требования к кодированию, которые и выполняют синусоиды/RoPE | `docs/03-deep-learning/04-attention-and-transformer.md` §6, вводный абзац | абзац |
| 19 | CNN в NLP: TextCNN (свёртки 2/3/4/5 + max-over-time как обучаемый детектор n-грамм), сравнение с attention по стоимости $O(knd^2)$ против $O(n^2d)$, пути между позициями и индуктивному смещению; когда CNN до сих пор берут | `docs/03-deep-learning/03-cnn-and-rnn.md` §12 | раздел (15–20 строк) |
| 20 | Сэмплеры min-p (порог как доля от вероятности лидера) и contrastive search (штраф за близость к уже сгенерированным скрытым состояниям) | `docs/05-llm/05-inference-and-serving.md` §11 | абзац |
| 21 | Назвать Tree-of-Thought и Graph-of-Thought своими именами и одной фразой отделить от self-consistency (ToT требует оценщика промежуточных шагов). Механизм описан верно, имён нет — кандидат не свяжет вопрос с прочитанным | `docs/05-llm/06-prompting-and-structured-output.md` §6.2 | 2–3 предложения |
| 22 | Процесс red teaming: матрица «категория вреда × вектор атаки» как определение покрытия, автоматический атакующий поверх ручного прогона, триаж по серьёзности, критерий выхода в релиз (ноль критических, ASR ниже порога), повторный прогон при смене версии модели | `docs/05-llm/10-llm-safety-and-guardrails.md`, новый подраздел после §5 | раздел |
| 23 | Право на забвение, когда данные в весах: machine unlearning не даёт гарантий и не принимается как комплаенс-ответ → псевдонимизация до обучения, привязка «пример датасета → субъект», выходной фильтр до релиза, каскадное удаление из индексов, логов и производных резюме | `docs/05-llm/10-llm-safety-and-guardrails.md` §7 | раздел |
| 24 | DCNv2 (матрица вместо вектора в кросс-слое, low-rank и MoE-версии) и xDeepFM (CIN, vector-wise против bit-wise у DCN — это и есть разделительная линия семейства) | `docs/10-recsys/05-factorization-machines.md` §6 | два абзаца |
| 25 | Семейство «гейт поверх признаков»: SENet/FiBiNet (squeeze-and-excitation, важность поля до кроссов), LHUC/MaskNet (поэлементная маска от пользователя или контекста); выигрыш — доли процента AUC почти бесплатно по латентности | `docs/10-recsys/08-neural-ranking.md`, новый подраздел между §3 и §4 | раздел |
| 26 | A/B для cold start: единица рандомизации — айтем, а не пользователь; метрика считается на холодном срезе выдачи; горизонт задаётся временем вызревания карточки, а не стандартными двумя неделями | `docs/10-recsys/15-recsys-online-evaluation.md` §6 | раздел |
| 27 | Меры близости для бинарного implicit: Jaccard ($|A\cap B|/|A\cup B|$, нормировка на объединение → жёстче штраф популярным) и Swing (гасит ко-встречаемость от одного «всеядного» пользователя). Все четыре разобранные меры выведены для явных оценок | `docs/10-recsys/03-collaborative-filtering.md` §2.2 | абзац |
| 28 | ISR, остальные четыре части вопроса: `unclean.leader.election.enable` и его цена, диагностика долго застрявшей вне ISR реплики (диск, сеть, GC), preferred leader election, перекос лидеров по брокерам | `docs/08-big-data/05-streaming.md` §2 | раздел |
| 29 | Протокол «под не шедулится»: `kubectl describe pod` → Events → `Insufficient cpu/memory`, несовпадение nodeSelector/affinity, unbound PVC, topology spread — и конкретное действие на каждую причину. Сейчас разобран только GPU-случай | `docs/07-mlops/06-docker-and-k8s.md` §15 | абзац + таблица причин |
| 30 | CAP и онлайн-стор фичей: при разделении Redis/DynamoDB/Cassandra выбирают доступность → «фича есть» ≠ «фича актуальна», отсюда обязательность TTL и метрики возраста; read-your-writes; лаг репликации между регионами как источник train/serve skew | `docs/07-mlops/03-data-and-feature-store.md` §6 | раздел |
| 31 | ClickHouse: `ORDER BY` как физическая сортировка, первичный ключ как разреженный индекс с засечкой на гранулу 8192 (и почему первым идёт поле низкой кардинальности), `PARTITION BY` как единица TTL, выбор ключа шардирования между хешем и локальностью джойна, семейство MergeTree и цена `FINAL`, мутации как асинхронная перезапись кусков | новая глава `docs/08-big-data/08-analytical-dbs.md` (вместе с работой №12) | глава |
| 32 | Immutable infrastructure и IaC: Terraform + Helm/Kustomize + ArgoCD как GitOps-контур, под не патчится, а пересоздаётся из образа по digest; выигрыш для ML — стенд и прод из одного репозитория, откат инфраструктуры как `git revert` | `docs/07-mlops/07-ci-cd-for-ml.md`, рядом с §8 | раздел |
| 33 | Transactional outbox: событие пишется в таблицу `outbox` в той же транзакции, что бизнес-изменение, релей или CDC (Debezium) публикует его — уходит пара багов «в БД записалось, в Kafka нет» и наоборот. Потребительская сторона в книге уже есть | `docs/08-big-data/05-streaming.md` §13 | абзац |
| 34 | Consistent hashing (при добавлении ноды переезжает ~$1/N$ ключей, зачем виртуальные ноды) и Saga как цепочка локальных транзакций с компенсациями вместо 2PC | `docs/07-mlops/05-serving-architectures.md` (или `11-system-design/01-framework.md`) | раздел |
| 35 | Класс для взвешенного сэмплирования: кумулятивные веса за $O(n)$ → `bisect_left` за $O(\log n)$; alias method Уокера как $O(1)$ на запрос ценой $O(n)$ предобработки; обновление весов через дерево Фенвика. Все ингредиенты в главе есть по отдельности, задачи нет; спрашивали в двух компаниях | `docs/12-coding/04-algorithms.md` §6 | задача с кодом |
| 36 | Язык Пёрла: рандомизация обрезает входящие рёбра в $T$, поэтому $P(Y \mid do(T)) = P(Y \mid T)$; в наблюдательных данных то же достигается блокировкой backdoor-путей — это unconfoundedness на графе. Механика в книге есть, терминов (do-оператор, backdoor, DAG) нет | `docs/02-classic-ml/17-uplift-and-causal.md` §3 | абзац |
| 37 | Задача с центрированным окном и исключением текущей строки: `(SUM(x) OVER (ORDER BY dt ROWS BETWEEN 2 PRECEDING AND 2 FOLLOWING) - x) / 4` либо `EXCLUDE CURRENT ROW`, с оговоркой `ROWS` против `RANGE` и про пропущенные даты | `docs/12-coding/05-sql-drills.md` §7, после задачи 17 | задача |
| 38 | Метод валидации суррогатных метрик: доля экспериментов с совпавшими знаками дельт, корреляция дельт, корреляция $t$-статистик по корпусу завершённых экспериментов; сколько экспериментов нужно; что делать при отсутствии истории (начать вести, поставить намеренно ухудшающие тесты с известным знаком). Сейчас — одно предложение в ответе «Проверь себя» | `docs/06-ab-testing/01-experiment-design.md` §3 (ссылка из `07-experiment-platform.md` §8) | раздел |
| 39 | Scaling laws рекомендательных моделей: HSTU и «всё поведение — одна последовательность токенов действий», retrieval и ranking как два режима одного трансдьюсера; впервые NLP-подобный рост качества с параметрами и длиной истории вместо плато; цена — инференс, переиндексация и невозможность применить продуктовые фильтры до генерации | `docs/10-recsys/09-sequential-recsys.md` после §10 и `11-llm-recsys.md` §5 | раздел |
| 40 | Решение МНК за один проход по блокам: копим $A \mathrel{+}= X_b^\top X_b$ и $b \mathrel{+}= X_b^\top y_b$, память $O(d^2)$ вместо $O(nd)$, одно чтение с диска, аддитивность → тривиальная параллелизация; решаем через Холецкого, а не явное обращение; при плохой обусловленности — инкрементальный QR или $\lambda I$. Якорь сильный: §15 уже учит аддитивным агрегатам по чанкам | `docs/12-coding/02-numpy-pandas.md` §15 (перелинк с `03-ml-from-scratch.md` §3) | подраздел |
| 41 | Кейс динамического ценообразования: таргет — спрос при цене, данные цензурированы прошлой политикой → эксплорационный слой по ценам; эластичность как ядро модели; оптимизация под маржу с guardrail на отток и регуляторику; рандомизация по гео-ячейке или switchback. Все восемь имеющихся кейсов — классификация и ранжирование, регрессионно-прогнозного нет | новая глава `docs/11-system-design/09-case-pricing-demand.md` | глава по 8-шаговому шаблону |

---

## P2 — приятно иметь

| # | Работа | Файл | Объём |
|---|---|---|---|
| 42 | Двумерное позиционное кодирование в ViT: 1D-таблица не знает, что патчи 1 и 15 — вертикальные соседи; при смене разрешения нужна bicubic-интерполяция по сетке $\sqrt N \times \sqrt N$; раздельные кодировки по строке и столбцу, axial RoPE | `docs/13-optional/01-computer-vision.md`, блок про ViT | абзац |
| 43 | Диффузионные языковые модели: итеративное размаскирование блока за фиксированное число шагов → латентность перестаёт линейно зависеть от длины ответа; платой идут длинные зависимости, отсутствие KV-кэша и незрелость движков. Экзотика, и честный ответ на собесе — «знаю механизм, в прод не ставил» | `docs/05-llm/01-llm-architecture.md`, короткий раздел в конце | раздел |
| 44 | Edge и mobile: ось cloud против on-device (приватность, офлайн, нулевая стоимость инференса против невозможности быстро обновить модель и зоопарка железа), рантаймы TFLite/LiteRT, Core ML, ExecuTorch, NNAPI, специфика (квантизация обязательна, батчинга нет, холодный старт = чтение весов с флеша). Роль-зависимый вопрос | `docs/07-mlops/09-inference-optimization.md` | раздел |
| 45 | DPP для диверсификации: вероятность подмножества как $\det(L_S)$ по матрице «качество × похожесть» → штрафуется похожесть всех пар списка сразу, а не только максимум к выбранным, как в MMR; жадный MAP-DPP стоит примерно как MMR. MMR в книге объяснён, DPP трижды назван и ни разу не раскрыт | `docs/10-recsys/14-recsys-in-production.md` §8 | абзац |
| 46 | Ограничения `mlflow.autolog()`: перехватывает только вызовы поддерживаемых фреймворков (кастомный цикл и препроцессинг вне `fit()` не видит), логирует параметры конструктора, а не эффективные значения, раздувает трекинг-сервер на длинных циклах, сигнатуру выводит не всегда → `infer_signature` и явный `log_model` обязательны | `docs/07-mlops/02-reproducibility-and-tracking.md` §5.1 | абзац |

---

## Как это читать при планировании

- **P0 распределён неравномерно по секциям**: `mlops`/`big-data` — 3 из 14 плюс самая тяжёлая
  работа (№13, крупный раздел) и единственная новая глава-необходимость (№12/31). Это совпадает
  с оценкой самих отчётов: у секции `mlops` худший счёт (ЕСТЬ 24 из 38 при 8 «НЕТ»), у
  `ab-sysdesign` и `llm` — лучший.
- **Дешёвых P0 семь из четырнадцати** (работы 3, 5, 11 — абзац или правка; 1, 6, 9, 10 — раздел
  на полстраницы). Работа №11 — правка неверного утверждения, её стоит сделать первой:
  это единственное место в сводке, где книга активно уводит кандидата в неправильный ответ.
- **Две новые главы** на весь список: `docs/08-big-data/08-analytical-dbs.md` (работы 12 и 31)
  и `docs/11-system-design/09-case-pricing-demand.md` (работа 41). Остальные 44 работы — правки
  внутри существующих файлов.
