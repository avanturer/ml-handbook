# Зависимости тренажёра. Версии не пришпилены жёстко: тесты не завязаны на детали API.
numpy>=1.24
scikit-learn>=1.3
pytest>=7.4

# Для задач из глав про PyTorch (в тренажёре не требуется):
# torch>=2.1
